# Backlog: Realtime SFU Migration

Replace PeerJS with Cloudflare Realtime SFU for cast-to-TV streaming.
Branch: `feat/realtime-sfu` (based on `feat/deploy-merged-api`)

---

- [ ] **1. Realtime SFU API client** — Create `services/api/src/lib/realtime.ts` with typed wrapper for CF Realtime HTTPS API. Functions: `createSession(offer)`, `addTracks(sessionId, tracks)`, `renegotiate(sessionId, answer)`, `closeTracks(sessionId, trackNames)`. TDD: write tests first with mocked fetch against the real API contract at `https://rtc.live.cloudflare.com/v1/apps/{appId}/sessions/...`. Env vars: `CLOUDFLARE_REALTIME_APP_ID`, `CLOUDFLARE_REALTIME_APP_SECRET`. Also add these to `services/api/src/types.ts` Env interface. Run `pnpm typecheck && pnpm lint && pnpm test` to verify.

- [ ] **2. SFU protocol types** — Update `services/api/src/protocol.ts` and `examples/stream-server-demo/container/src/protocol.ts` and `services/api/container/src/protocol.ts`. Add SFU-specific types: `PublisherPrepareRequest` (url, iceServers), `PublisherPrepareResponse` (sessionDescription, tracks, traceId), `PublisherAnswerRequest` (sessionDescription), `SubscribeRequest` (publisherSessionId), `SubscribeResponse` (sessionDescription, sessionId), `SubscribeAnswerRequest` (sessionDescription, subscriberSessionId)`. Keep existing types that are still used (IceServerConfig, parseTurnCredentialsResponse). Remove PeerJS-specific `StartStreamRequest` and `parseStartStreamRequest`. Run feedback commands.

- [ ] **3. Update Chrome extension — replace PeerJS with RTCPeerConnection** — In `examples/stream-server-demo/container/extension/streaming.js` (canonical) and then copy to `services/api/container/extension/streaming.js`. Keep all `chrome.tabCapture` capture logic unchanged. Replace the PeerJS block (around line 644) with: `INITIALIZE_PUBLISHER({ iceServers })` that creates `RTCPeerConnection`, adds captured tracks with fixed names `cast-video`/`cast-audio`, creates local SDP offer, returns `{ sessionDescription: pc.localDescription, tracks: [{...}] }`. Add `APPLY_REMOTE_DESCRIPTION(answer)` that calls `pc.setRemoteDescription(answer)`. Add `CLOSE_PUBLISHER()` for cleanup. Remove all `Peer`/PeerJS references. Remove `peer.js` script from `streaming.html`. Remove `peerHost`/`peerPort` from INITIALIZE params. Update `window.activeConnections` tracking to use `pc.connectionState` instead of PeerJS events.

- [ ] **4. Update container server — split start-stream into prepare/answer** — In `examples/stream-server-demo/container/src/server.ts` (canonical) and copy to `services/api/container/src/server.ts`. Replace `POST /start-stream` with two endpoints: `POST /publisher/prepare` (launches Chrome if needed, navigates to URL, captures tab, calls INITIALIZE_PUBLISHER in extension, returns local SDP offer + track info) and `POST /publisher/answer` (passes SFU answer to extension via APPLY_REMOTE_DESCRIPTION, returns success). Remove all `peerId`, `destPeerId`, `peerHost`, `peerPort` handling. Keep `PEERJS_HOST`/`PEERJS_PORT` env var cleanup. Add `GET /publisher/state` for debugging. Run feedback commands.

- [ ] **5. Update StreamContainer DO — SFU session coordinator** — In `services/api/src/stream-container.ts`: add state storage for `publisherSessionId`, `publisherTracks` (track manifest), `subscriberSessions` (Map of receiver session IDs). Route new paths through container proxy: `/publisher/prepare`, `/publisher/answer`, `/publisher/state`, `/subscribe`, `/subscribe/:id/answer`. The DO itself doesn't call the Realtime API — it proxies to the container for publisher ops and handles subscriber ops via the worker routes. Run feedback commands.

- [ ] **6. Update cast/stream API routes — SFU negotiation endpoints** — In `services/api/src/routes/stream.ts`: Add `POST /subscribe` (creates subscriber Realtime session via lib/realtime.ts, subscribes to publisher tracks, returns SDP offer to receiver). Add `PUT /subscribe/:subscriberSessionId/answer` (renegotiates with receiver's SDP answer). Update `/start-stream` to use new two-phase flow: call container `/publisher/prepare` to get local offer, call Realtime API to create session + add tracks, call container `/publisher/answer` with SFU's answer. In `services/api/src/routes/cast.ts`: update the cast stream proxy to route new SFU endpoints. Backend owns ALL Realtime API calls — APP_SECRET never exposed to client/extension. Run feedback commands.

- [ ] **7. Update receiver pages — replace PeerJS with native WebRTC** — In `examples/cast-receiver/receiver.html` and `examples/stream-server-demo/receiver.html`: Remove PeerJS CDN `<script>` tag. Replace PeerJS flow with: fetch `/ice-servers` for TURN config, call backend `POST /subscribe` to get SDP offer, create `RTCPeerConnection` with ICE servers, set remote offer, create local answer, POST answer to backend `PUT /subscribe/:id/answer`, listen for `track` event on pc, attach remote `MediaStream` to `<video>` element. Remove all `Peer`, `peerHost`, `peerPort` references. Keep status overlay UI, connection timeout logic, and error handling.

- [ ] **8. Update e2e tests and verify** — Update `services/api/test/e2e/cast-stream.spec.ts`: change WebRTC video tests to use new SFU flow (no PeerJS server needed, no peerHost/peerPort). Update `examples/stream-server-demo/test/e2e.spec.ts`: remove `test.fixme()`, SFU signaling should be deterministic (no 0.peerjs.com dependency). Update CI workflows: remove PeerJS server startup from `ci-api-e2e.yml`, remove `PEERJS_HOST`/`PEERJS_PORT` env vars. Run full `pnpm typecheck && pnpm lint && pnpm test`. Run `/codex-review --base feat/deploy-merged-api` for final review.
