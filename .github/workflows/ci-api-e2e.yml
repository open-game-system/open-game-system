name: CI - API E2E (Cast Streaming)

on:
  push:
    branches: [main]
    paths:
      - "services/api/**"
      - "examples/cast-receiver/**"
      - "examples/stream-server-demo/**"
      - ".github/workflows/ci-api-e2e.yml"
  pull_request:
    branches: [main]
    paths:
      - "services/api/**"
      - "examples/cast-receiver/**"
      - "examples/stream-server-demo/**"
      - ".github/workflows/ci-api-e2e.yml"

jobs:
  cast-e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      # Install Puppeteer's Chrome for stream server
      - name: Install Chrome for Puppeteer
        working-directory: examples/stream-server-demo/container
        run: |
          pnpm install
          npx puppeteer browsers install chrome

      # Install Playwright browsers for E2E tests
      - name: Install Playwright
        working-directory: services/api
        run: npx playwright install chromium

      # Set up virtual display for Chrome (stream server needs it for tab capture)
      - name: Set up Xvfb
        run: |
          sudo apt-get update -q
          sudo apt-get install -y xvfb
          Xvfb :99 -screen 0 1920x1080x24 &
          echo "DISPLAY=:99" >> $GITHUB_ENV

      # Start local PeerJS signaling server (avoids flaky 0.peerjs.com)
      - name: Start PeerJS server
        run: |
          npx peer --port 9000 &
          # Wait for PeerJS server to be ready (npx needs to download first)
          for i in $(seq 1 30); do
            if curl -s http://localhost:9000/ > /dev/null 2>&1; then
              echo "PeerJS server ready after ${i}s"
              break
            fi
            sleep 1
          done
          curl -s http://localhost:9000/ || echo "WARNING: PeerJS server may not be ready"

      # Start stream server in background (after Xvfb)
      - name: Start stream server
        working-directory: examples/stream-server-demo/container
        env:
          PEERJS_HOST: localhost
          PEERJS_PORT: "9000"
        run: |
          npx tsx src/server.ts &
          sleep 10
          curl -s http://localhost:8080 || echo "Stream server started"

      # Create local D1 database with schema
      - name: Set up local D1
        working-directory: services/api
        run: |
          echo 'JWT_SECRET="ci-jwt-secret"' > .dev.vars
          echo 'ENVIRONMENT="ci"' >> .dev.vars
          echo 'STREAM_SERVER_URL="http://localhost:8080"' >> .dev.vars
          npx wrangler d1 execute opengame-api-db --local --file=schema.sql
          npx wrangler d1 execute opengame-api-db --local --command "INSERT INTO api_keys (key, game_id, game_name) VALUES ('ci-test-key', 'test-game', 'CI Test Game')"

      # Start API server in background
      - name: Start API server
        working-directory: services/api
        run: |
          npx wrangler dev --port 8788 --inspector-port 0 &
          sleep 5
          curl -s http://localhost:8788/api/v1/health

      # Serve cast-receiver over HTTP (file:// blocks fetch to localhost)
      - name: Start receiver HTTP server
        run: |
          npx http-server examples/cast-receiver -p 8888 --cors -s &
          for i in $(seq 1 30); do
            if curl -s http://localhost:8888/receiver.html > /dev/null 2>&1; then
              echo "Receiver HTTP server ready after ${i}s"
              break
            fi
            sleep 1
          done

      # Pre-warm Chrome browser (first launch with extensions is slow)
      - name: Warm up stream server Chrome
        run: |
          echo "Triggering Chrome warm-up via /start-stream..."
          curl -s -X POST http://localhost:8080/start-stream \
            -H "Content-Type: application/json" \
            -d '{"url":"https://example.com","peerId":"warmup-peer"}' \
            --max-time 90 || true
          echo "Chrome warm-up complete"

      # Run E2E tests
      - name: Run cast E2E tests
        working-directory: services/api
        run: npx playwright test test/e2e/cast-stream.spec.ts --reporter=list
        env:
          E2E_API_URL: http://localhost:8788
          E2E_API_KEY: ci-test-key
          E2E_STREAM_SERVER_URL: http://localhost:8080
          E2E_RECEIVER_BASE_URL: http://localhost:8888
          PEERJS_HOST: localhost
          PEERJS_PORT: "9000"

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cast-e2e-results
          path: services/api/test-results/
          retention-days: 7
